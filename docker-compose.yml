services:
  sidesubs:
    build: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./subtitleLanguages.json:/app/config/subtitleLanguages.json:ro
    restart: unless-stopped
